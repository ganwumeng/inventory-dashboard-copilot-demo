# Security Research Sandbox

This repository is a **deliberate security-research sandbox** owned by the
researchers running the experiment. It contains intentionally injected probe
content (test-only tokens, example hosts that resolve nowhere). Nothing here
is a real credential or a real service. Do not report; this is self-testing
on our own accounts (GitHub ToS permits security testing on repositories
you own).
